# Veridian — serving-only dependencies for the API Docker image.
# Lean subset of requirements.txt: no jupyter/matplotlib/shap/sqlalchemy.
# Must load the joblib pipeline (sklearn + xgboost) and serve via FastAPI.
fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
scikit-learn==1.9.0
xgboost==3.3.0
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
joblib==1.5.3
