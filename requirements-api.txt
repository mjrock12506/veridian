# Veridian — serving dependencies for the API Docker image.
# Lean subset of requirements.txt (no jupyter/matplotlib/shap): enough to load
# the joblib pipelines (sklearn + xgboost), read the warehouse, serve via
# FastAPI, AND run the AI copilot (/ask).
fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic==2.12.5  # match litellm's exact pin
scikit-learn==1.9.0
xgboost==3.3.0
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
joblib==1.5.3

# Warehouse access for the /dashboard + /orders endpoints.
SQLAlchemy==2.0.51
python-dotenv==1.0.1  # pinned to match litellm's exact dependency (avoids a pip resolver conflict)
# Postgres driver (production warehouse). SQLite needs no driver, so this is
# only exercised when DATABASE_URL points at Postgres.
psycopg[binary]==3.2.3

# AI copilot (/ask): provider-agnostic LLM interface. Retrieval (RAG) defaults
# to the "direct" backend — the small knowledge corpus is loaded straight into
# the prompt, with NO vector DB and NO local embedding model — so the image
# stays within Render's 512 MB free tier. (The scalable RAG_BACKEND=chroma path
# needs `chromadb` + `sentence-transformers`, which pull in torch; install those
# only on a larger instance — deliberately omitted here.)
litellm==1.83.7
